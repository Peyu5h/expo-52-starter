import { View } from 'react-native';
import { Text } from '~/components/ui/text';

export default function InMobileRedirectionScreen() {
  return (
    <View className="flex-1 justify-center items-center p-4">
      <Text className="text-xl">Call number screen, mail, redirect to whatsapp, spotify</Text>
    </View>
  );
}
